<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville&family=News+Cycle&family=Roboto:wght@100&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="/styles/main.css">
        <title>Dalessi News</title>
    </head>
    <body>
        <h1>Dalessi News</h1>
        <form action="/" method="post">
            <input type="text" name="search" id="search">
            <input type="submit" value="Search">
        </form>
        
            
            <% if(locals.data) { %>
                <p>Total results: <%= numberOfResults %></p>
                <div class="container">
                    <% for(let i = 0; i < data.length; i++ ) { %>
                        <div class="card">
                            <div class="card-body">
                                <img src="<%= data[i].urlToImage %>" />
                                <a href="<%= data[i].url %>"><h3><%= data[i].title %></h3></a>
                                <p><%= data[i].publishedAt.slice(0,10) %></p>
                                <p><%= data[i].description %></p>
                            </div>
                        </div>
                    <% } %>
                </div>
            <% } else { %>
                <p>No results found. Enter a search result.</p>
            <% } %>
    </body>
</html>